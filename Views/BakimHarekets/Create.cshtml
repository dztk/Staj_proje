@model Proje.Models.BakimHareket

@{
    ViewData["Title"] = "Ekle";
}

<h1>Ekle</h1>

<h4>Hareket Bakım</h4>
<hr /> 
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="text-danger">
                    @foreach (var modelError in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <div>@modelError.ErrorMessage</div>
                    }
                </div>
            }


            @* <div class="form-group">
                <label asp-for="IsEmriId" class="control-label"></label>
                <select asp-for="IsEmriId" class ="form-control" asp-items="ViewBag.IsEmriId"></select>
            </div> *@

            <div class="form-group">
                <label asp-for="IsEmriId" class="control-label">İş Emri</label>
                <select asp-for="IsEmriId" class="form-control" asp-items="ViewBag.IsEmriId">
                    <option value="">-- İş Emri Seçin --</option>
                </select>
                <span asp-validation-for="IsEmriId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="YapilanIslem" class="control-label"></label>
                <input asp-for="YapilanIslem" class="form-control" />
                <span asp-validation-for="YapilanIslem" class="text-danger"></span>
            </div>
           @*  <div class="form-group">
                <label asp-for="Tarih" class="control-label"></label>
                <input asp-for="Tarih" class="form-control" type="date" /> <!--type="date"-->
                <span asp-validation-for="Tarih" class="text-danger"></span>
            </div> *@
            @* <div class="form-group">
                <label asp-for="PersonelId" class="control-label"></label>
                <select asp-for="PersonelId" class ="form-control" asp-items="ViewBag.PersonelId"></select>
            </div> *@
            @* Personel ID sistem tarafından atanacak, formda gösterilmeyecek *@
            <div class="form-group">
                <label class="control-label">Teknisyen</label>
                <input class="form-control" value="@User.Identity.Name" disabled />
            </div>
            <div class="form-group">
                <input type="submit" value="Ekle" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Listeye Dön</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
